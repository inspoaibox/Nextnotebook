# 同步锁问题排查指南

## 问题现象

同步失败，错误信息：
```
Failed to acquire sync lock - another device may be syncing
```

## 原因分析

1. **正常情况**：另一台设备正在同步，需要等待
2. **异常情况**：
   - 上次同步时应用崩溃，锁未正常释放
   - 切换到新的 WebDAV 服务器，旧锁仍然存在
   - 网络中断导致锁释放失败

## 解决方案

### 方案 1：等待锁自动过期（推荐）

同步锁有超时机制（默认 2 分钟），等待几分钟后会自动释放。

### 方案 2：强制释放锁

**⚠️ 警告：仅在确认没有其他设备正在同步时使用！**

#### 桌面端操作步骤：

1. 打开 **设置** → **同步设置**
2. 滚动到底部 **高级操作** 区域
3. 点击 **强制释放同步锁** 按钮
4. 确认操作

#### Android 端操作步骤：

1. 打开 **设置** → **同步设置**
2. 滚动到底部 **高级操作** 区域
3. 点击 **强制释放同步锁** 按钮
4. 确认操作

### 方案 3：切换服务器时的处理

如果你切换到了新的 WebDAV 服务器或同步服务器：

1. 先在旧服务器上正常同步一次（如果可以访问）
2. 切换到新服务器
3. 使用 **强制释放锁** 清理可能存在的旧锁
4. 重新同步

## 预防措施

1. **避免多设备同时同步**：等待一台设备同步完成后再在另一台设备上同步
2. **网络稳定时同步**：避免在网络不稳定时进行同步
3. **正常退出应用**：不要强制关闭应用，让同步正常完成

## 技术细节

### 锁的工作原理

- 每次同步开始时，设备会尝试在远端创建一个锁文件
- 锁文件包含：设备 ID、创建时间、过期时间
- 同步完成后，设备会删除锁文件
- 如果锁已存在且未过期，其他设备无法同步

### 强制释放锁的实现

- **WebDAV**：直接删除 `.mucheng-sync/locks/lock.json` 文件
- **自建服务器**：调用 `DELETE /api/sync/lock/force` 接口

## 常见问题

### Q: 强制释放锁会丢失数据吗？

A: 不会。强制释放锁只是删除锁文件，不会影响已同步的数据。但如果有其他设备正在同步，可能会导致冲突。

### Q: 多久会自动释放锁？

A: 默认 2 分钟。如果设备在同步过程中崩溃，锁会在 2 分钟后自动过期。

### Q: 可以禁用同步锁吗？

A: 不建议。同步锁是为了防止多设备并发同步导致数据冲突。

## 自动锁过期检测

从 v1.0.0 开始，应用会在同步前自动检测锁状态：

1. **自动清理过期锁**：如果检测到锁已过期，会自动清理并重新获取
2. **显示锁所有者**：如果锁被其他设备持有，会显示设备 ID
3. **智能重试**：过期锁会被自动清理，无需手动干预

这意味着大多数情况下，你不需要手动强制释放锁。只有在以下情况才需要手动操作：

- 确认其他设备已停止同步，但锁仍然存在
- 切换到新的同步服务器
- 锁文件损坏

## 更新日志

- 2026-01-06：添加桌面端和 Android 端强制释放锁功能
- 2026-01-06：添加自动锁过期检测和清理功能

